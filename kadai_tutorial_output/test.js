// ランダムなテキストを表示
const createText = () => {};

// キー入力の判定
const keyPress = e => {
    console.log(e.key);
};

// タイピングスキルのランクを判定
const rankCheck = score => {};

// ゲームを終了
const gameOver = id => {};

// カウントダウンタイマー
const timer = () => {};
